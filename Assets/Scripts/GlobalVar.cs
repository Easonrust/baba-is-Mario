using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalVar : MonoBehaviour
{
    public static int playerCtrlNum = 0;
    public static bool levelWin = false;
    public static bool changeScene = false;
    public static bool bossLevel = false;
    public static bool roundStart = false;
    public static bool bossSpeaking = false;
    public static bool throwing = false;
    public static bool roundWin = false;
    public static bool roundLose = false;
    public static bool bossIsYou = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
